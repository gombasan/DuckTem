<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ducktem.web.dao.SearchDao">

    <select id="getList" resultType="com.ducktem.web.entity.ProductPreview">
        select pi.name thumbNailImg, p.price, p.regDate, p.id productId ,p.name
		from Product_image pi 
		inner join Product p on pi.productId = p.id
	        where p.name like '%${keyword}%' and pi.thumbNail = 1
	    	<if test="${option} == null || #{option} == 'latest'">
	       		order by p.regDate desc
	        </if>
	    	<if test="#{option} == 'price-high'">
	       		order by p.price desc
	        </if>
	    	<if  test="#{option} == 'price-low'">
	       		order by p.price asc
	        </if>        
	    	<if  test="#{option} == 'most-viewed'">
	       		order by p.hit desc
	        </if>        
    </select>

    <select id="count" resultType="int">
        select count(p.id)
		from Product p
		inner join Product_image pi on p.id = pi.productId
        where p.name like '%${keyword}%' and pi.thumbNail = 1
    </select>

</mapper>