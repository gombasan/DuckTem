<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ducktem.web.dao.SearchDao">

    <select id="getList" resultType="com.ducktem.web.entity.ProductPreview">
        select pi.name thumbNailImg, p.price, p.regDate, p.id productId ,p.name
		from Product_image pi 
		inner join Product p on pi.productId = p.id
        where p.name like '%${keyword}%'
        order by regDate desc
    </select>

    <select id="count" resultType="int">
        select count(id)
		from Product 
        where name like '%${keyword}%'
    </select>

</mapper>